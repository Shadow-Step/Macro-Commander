<Window x:Class="Macro_Commander.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Macro_Commander"
        xmlns:statics="clr-namespace:Macro_Commander.res"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Closing="Window_Closing" Loaded="Window_Loaded">
    <Window.Resources>
        <local:ColorConverter x:Key="BorderBrushConverter"/>
        <local:ActionTypeConverter x:Key="ActionTypeConverter"/>

        <DataTemplate x:Key="ListBoxUnselected">
            <Border Width="125" CornerRadius="4" BorderThickness="2" BorderBrush="{Binding Path=ActionType, Converter={StaticResource BorderBrushConverter}}" Background="#3d3d3d" Margin="2 2 2 2">
                <StackPanel Orientation="Horizontal">
                    <Border x:Name="Brd" BorderThickness="1" BorderBrush="Black" CornerRadius="4">
                        <Image Margin="1 1 1 1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Source="{Binding Path=Bitmap}"/>
                    </Border>
                    <Separator/>
                    <StackPanel Orientation="Vertical" Margin="2 0 2 0">
                        <TextBlock FontWeight="Bold" Foreground="LightGray" Text="{Binding StringFormat=X: {0}, Path=X}"/>
                        <TextBlock FontWeight="Bold" Foreground="LightGray" Text="{Binding StringFormat=Y: {0}, Path=Y}"/>
                        <TextBlock FontWeight="Bold" Foreground="LightGray" Text="{Binding StringFormat=P: {0}, Path=Pause}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="ListBoxSelected">
            <Border Width="125" CornerRadius="4" BorderThickness="2" BorderBrush="Red" Background="#3d3d3d" Margin="2 2 2 2">
                <StackPanel Orientation="Horizontal">
                    <Border x:Name="Brd" BorderThickness="1" BorderBrush="Black" CornerRadius="4">
                        <Image Margin="1 1 1 1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Source="{Binding Path=Bitmap}"/>
                    </Border>
                    <Separator/>
                    <StackPanel Orientation="Vertical" Margin="2 0 2 0">
                        <TextBlock FontWeight="Bold" Foreground="Red" Text="{Binding StringFormat=X: {0}, Path=X}"/>
                        <TextBlock FontWeight="Bold" Foreground="Red" Text="{Binding StringFormat=Y: {0}, Path=Y}"/>
                        <TextBlock FontWeight="Bold" Foreground="Red" Text="{Binding StringFormat=P: {0}, Path=Pause}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </DataTemplate>
        <Style x:Key="ListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="IsEnabled" Value="False"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=SelectedMacro.StartedMutex}" Value="False">
                    <Setter Property="IsEnabled" Value="True"/>
                </DataTrigger>
            </Style.Triggers>
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#4d4d4d"/>
            </Style.Resources>
        </Style>
        <Style x:Key="ListBoxItems" TargetType="ListBoxItem">
            <Setter Property="ContentTemplate" Value="{StaticResource ListBoxUnselected}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="ContentTemplate" Value="{StaticResource ListBoxSelected}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="MenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="LightGray"/>
            <Setter Property="Background" Value="#2d2d2d"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4d4d4d"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <ControlTemplate x:Key="ButtonTemplate" TargetType="Button">
            <Border Name="Brd" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" BorderThickness="1" BorderBrush="#3d3d3d" Background="#2d2d2d">
                <ContentPresenter RecognizesAccessKey="True"  Margin="{TemplateBinding Padding}"></ContentPresenter>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Brd" Property="Background" Value="#4d4d4d"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="Brd" Property="Background" Value="#1d1d1d"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        
    </Window.Resources>
    <Grid Background="#2d2d2d">
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>

        <Menu VerticalAlignment="Top" Grid.Row="0" ItemContainerStyle="{StaticResource MenuItemStyle}" Height="25" Background="#2d2d2d">
            <MenuItem Header="File">
                <MenuItem Header="Load" Click="MenuItem_Click"/>
                <MenuItem Header="Save" Click="MenuItem_Click_1"/>
            </MenuItem>
            <MenuItem Header="Options"/>
            <MenuItem Header="View"/>
            <MenuItem Header="Help"/>
        </Menu>
        <Menu Background="#2d2d2d" Grid.Row="1" Height="25"/>

        <Grid Grid.Row="2" Margin="5 5 5 5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <GroupBox Grid.Column="0" Grid.Row="0" Header="Options">

            </GroupBox>
            <ListBox Grid.Row="1" Grid.Column="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Style="{StaticResource ListBoxStyle}" ItemContainerStyle="{StaticResource ListBoxItems}" ItemsSource="{Binding Path=SelectedMacro.Actions}" SelectedItem="{Binding Path=SelectedMacro.SelectedAction}">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
            <GroupBox Grid.Column="1" BorderThickness="1" BorderBrush="#3d3d3d" Grid.RowSpan="2">
                <GroupBox.Header>
                    <TextBlock Text="Browse" Foreground="LightGray"/>
                </GroupBox.Header>
                <StackPanel Orientation="Vertical">
                    <ListBox ItemsSource="{Binding Path=MacroList}" MinHeight="200" SelectedItem="{Binding Path=SelectedMacro}" DisplayMemberPath="Name"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Create" Command="{Binding Path=CommandAddMacro}"/>
                        <Button Content="Delete" Command="{Binding Path=CommandDelMacro}" CommandParameter="{Binding Path=SelectedMacro}"/>
                    </StackPanel>
                    <Separator/>
                    <ComboBox ItemsSource="{Binding Source={x:Static statics:Statics.ActionDictionary}, Path=Values}" SelectedItem="{Binding Path=SelectedMacro.SelectedAction.ActionType, Converter={StaticResource ActionTypeConverter}}"/>
                    <TextBox Text="{Binding Path=SelectedMacro.SelectedAction.X}"/>
                    <TextBox Text="{Binding Path=SelectedMacro.SelectedAction.Y}"/>
                    <TextBox Text="{Binding Path=SelectedMacro.SelectedAction.Pause}"/>
                </StackPanel>
            </GroupBox>
        </Grid>

        <StatusBar Grid.Row="3" Background="#3d3d3d" VerticalAlignment="Bottom" Height="30"/>
    </Grid>
</Window>
